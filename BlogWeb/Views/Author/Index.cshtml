@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model IEnumerable<BlogWeb.Models.AdminAccount>
@{
     Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container">
     <form asp-controller="Author" asp-action="Index" class="d-none d-md-inline-block form-inline ml-auto mr-0 mr-md-3 my-2 my-md-0">
        <div class="input-group mb-3">
             <input name="name" type="text" value="@ViewBag.Name" class="form-control" placeholder="Search authors for..." />
            <div class="input-group-append">
                <button class="btn btn-outline-secondary" style="padding: 0 10px 0 10px">Search</button>
            </div>
        </div>
    </form>
      <div class="row align-content-center">
            @foreach (var author in Model){
            <div class="col-lg-4 col-md-3 mb-4">
                <div class="card border-0 shadow">
                    <div class="card-body">
                        <div class="row text-center" style="margin: 0px auto 10px 0px;">
                            <h5 class="card-title text-center w-100"><a asp-controller="Author" asp-action="Detail" asp-route-id="@author.AdminAccountId">@author.FullName</a></h5>
                        </div>
                    </div>
                </div>
            </div>  
            }
      </div>   
</div>
